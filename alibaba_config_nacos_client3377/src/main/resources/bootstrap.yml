server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #Nacos作为服务注册中心
      config:
        server-addr: localhost:8848 #Nacos作为配置中心
        file-extension: yaml #指定yaml格式的配置
        #group: TEST_GROUP #分组
        #namespace:        #命名空间

# ${spring.application.name}-${spring.profile.activate}.${spring.cloud.nacos.config.file-extension}
